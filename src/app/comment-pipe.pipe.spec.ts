import { CommentPipePipe } from './comment-pipe.pipe';

describe('CommentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CommentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
